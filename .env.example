# Copy to .env and adjust if needed. Used by docker-compose (backend uses host "db" internally).
# For local backend dev, use DATABASE_URL with host localhost:5432.
POSTGRES_USER=inventory_user
POSTGRES_PASSWORD=inventory_pass
POSTGRES_DB=inventory_db
DATABASE_URL=postgresql://inventory_user:inventory_pass@localhost:5432/inventory_db
